services:
  airsonic:
    image: lscr.io/linuxserver/airsonic-advanced
    container_name: airsonic
    environment:
      - PUID=990
      - PGID=991
      - TZ=America/Toronto
      # - CONTEXT_PATH=/airsonic
    volumes:
      - /data/containers/airsonic/config:/config
      - /d/copper/audio/music:/audio/music:ro
      - /d/copper/audio/comedy:/audio/comedy:ro
      - /d/copper/audio/playlists:/playlists
      - /d/copper/audio/podcasts:/audio/podcasts:ro
      - /d/copper/audio/podcasts_airsonic:/podcasts
      # - ./run:/etc/services.d/airsonic/run
    network_mode: host
    # ports:
    #   - 4040:4040
    #   - 4041:4041
    restart: unless-stopped
    labels:
      - "LABEL com.centurylinklabs.watchtower.enable=true"
